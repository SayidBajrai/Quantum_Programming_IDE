// Bell state example
qbit[2] q
bit[2] c

gate Bellgate(a, b) {
    H(a)
    CNot(a, b)
}

// before Bell gate
Print(f"qubits before Bell gate: {q}")

// after Bell gate
Bellgate(q[0], q[1])
Print(f"qubits after Bell gate: {q}") 

// measure
Measure(q, c)